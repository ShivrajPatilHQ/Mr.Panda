name: Build installers

# Builds each platform on its own real runner — a Windows installer built under
# Wine on a Mac is not something anyone has actually run. Trigger it by hand, or
# push a v* tag to attach the results to that release.
on:
  workflow_dispatch:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            script: dist:win
            artifacts: dist/*.exe
          - os: ubuntu-latest
            script: dist:linux
            artifacts: dist/*.AppImage
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci || npm install

      # --publish never is required: on CI electron-builder otherwise tries to
      # publish a release itself and dies with "GitHub Personal Access Token is
      # not set". We attach artifacts explicitly below instead, so a failed
      # build can never overwrite a good release.
      - name: Build
        run: npm run ${{ matrix.script }} -- --publish never
        env:
          CSC_IDENTITY_AUTO_DISCOVERY: false

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: mrpanda-${{ matrix.os }}
          path: ${{ matrix.artifacts }}
          if-no-files-found: error

      - name: Attach to release (tag builds only)
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.artifacts }}
